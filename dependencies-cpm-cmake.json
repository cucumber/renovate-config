{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Update version variables in dependencies.cpm.cmake",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update version variables in dependencies.cpm.cmake. The format should look like this\n```\n# renovate: datasource=github-tags packageName=<org/repo> versioning=semver\nset(<DEPENDENCY_NAME>_HASH_VERSION <digest> <version>)\n```",
      "managerFilePatterns": [
        "/(^|/)dependencies.cpm.cmake$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+?)(?: depName=(?<depName>.+?))? packageName=(?<packageName>.+?)(?: versioning=(?<versioning>[a-z-]+?))?\\sset(.+?_HASH_VERSION (?:<currentDigest>.+?) (?:<currentValue>.+?))"
      ]
    }
  ]
}
