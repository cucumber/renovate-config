{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Update version variables in dependencies.cpm.cmake",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update version variables in dependencies.cpm.cmake",
      "managerFilePatterns": [
        "/(^|/)dependencies.cpm.cmake$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+?)(?: depName=(?<depName>.+?))? packageName=(?<packageName>.+?)(?: versioning=(?<versioning>[a-z-]+?))?\\sset\\(.+? (?<currentValue>.+?) CACHE STRING \".+?\"\\)"
      ]
    }
  ]
}
